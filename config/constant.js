// Param of quoter fn call
const sqrtPriceLimitX96 = 0;

// Tolerance is represented in decimal, if your price tolerance is 1% then set tolerance to 0.01
const tolerance = 0.0001;

const startAmount = 0;
const endAmount = 1000;

module.exports = { sqrtPriceLimitX96, tolerance, startAmount, endAmount };
